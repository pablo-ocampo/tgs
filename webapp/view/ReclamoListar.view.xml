<mvc:View
	controllerName="neo.tgs.controller.ReclamoListar"
	xmlns="sap.m"
	xmlns:mvc="sap.ui.core.mvc">
	<Page title="Listado de reclamos">
		<headerContent>
			<Button icon="sap-icon://action" tooltip="Share" />
		</headerContent>
		<subHeader>
			<OverflowToolbar>
				<Breadcrumbs currentLocationText="Listado" separatorStyle="GreaterThan">
					<Link press="onPress" text="Inicio"/>
					<Link press="onPress" text="Reclamos"/>
				</Breadcrumbs>
			</OverflowToolbar>
		</subHeader>
		<content>
			<Text text="Un simple texto que indica que estás en el listado de reclamos"/>
		</content>
	</Page>
</mvc:View>